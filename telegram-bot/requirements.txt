# --- Telegram / async core ---
aiogram==3.15.0            # современный async-фреймворк с FSM из коробки

# --- База данных (async) ---
SQLAlchemy[asyncio]==2.0.36
aiosqlite==0.20.0          # async-драйвер SQLite для MVP
alembic==1.14.0            # миграции (готовы к переезду на PostgreSQL)
# asyncpg==0.30.0          # раскомментировать при переходе на PostgreSQL

# --- Конфигурация / секреты ---
# aiogram 3.15 требует pydantic<2.10 — держим совместимые версии
pydantic==2.9.2
pydantic-settings==2.6.1   # чтение .env с валидацией типов

# --- Интеграция с CRM (Google Sheets) ---
gspread==6.1.4
google-auth==2.37.0

# --- Прочее ---
tenacity==9.0.0            # retry с backoff для внешних вызовов
